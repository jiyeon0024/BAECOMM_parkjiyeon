import React from "react";
import styled from "styled-components";

const StyledLogo = styled.h2`
  color: #10b981;
`;

const Logo = () => {
  return <StyledLogo>BAECOMM</StyledLogo>;
};

export default Logo;
